def call() {
pipeine{
    agent any
    stages {
        stage ('git') {
            steps git url: 'github.com/spring-projects/spring-petclinic.git',
            branch: 'main'
        }
        stage ('build') {
            sh 'mvn clean package'
        }
        }
    }
}
